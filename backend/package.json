{
  "name": "portal-limiar-backend",
  "private": true,
  "version": "1.0.0",
  "description": "Apps Script + Google Drive backend for Portal Limiar",
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  },
  "scripts": {
    "login": "clasp login",
    "push": "clasp push",
    "open": "clasp open-script",
    "deploy": "clasp deploy"
  }
}
